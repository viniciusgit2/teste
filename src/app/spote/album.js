
import {tojson} from "util"

import { api_url,headher } from "./config"

export const getalbum=()=> fetch( `${api_url},headher(tojson`)
.then(tojson)
export const getalbumtracks=()=>`${api_url}(tojson)`
.then(tojson)
export const getalbums=()=>`${api_url}(tojson)`